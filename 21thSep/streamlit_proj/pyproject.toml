[project]
name = "streamlit-proj"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "AqsaNabi", email = "miraqsa186@gmail.com" }
]
requires-python = ">=3.10"
dependencies = []

[project.scripts]
streamlit-proj = "streamlit_proj:main"

[build-system]
requires = ["uv_build>=0.12.17,<0.13.0"]
build-backend = "uv_build"
